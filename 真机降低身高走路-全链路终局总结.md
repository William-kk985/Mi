# 真机降低身高走路 — 全链路终局总结

> 2026-08-09/10，25+条路线，穷举 CyberDog2 真机 NX+RT 板所有通信链路

## 一、核心结论

**真机普通 trot 走路时，无法通过外部命令动态降低身体高度。**

- LocoGaits（普通 trot 控制器）的身高源 = `des_roll_pitch_height[2]` 用户参数
- 写该参数 → RT 板崩溃（ControlParameter::Set 无锁循环写 vs LocoGaits 500Hz 读 → 撕裂 → MPC NaN）
- 所有命令级通道（pos_des[2]/body_height/value=0x02）LocoGaits 固件均不读取
- MotionGaits 参数安全但 kTrotInOut(56)=普通 trot 不走，kWalkWave(60)=能走但波浪

## 二、两大控制器对比

| | LocoGaits | MotionGaits |
|---|---|---|
| **步态范围** | gait_id ≤ 49（含 trot=3） | gait_id ≥ 50（56/60/80/110等） |
| **走路稳定性** | ✅ 普通trot 稳走 1.27m | ❌ 56 原地踏步 0.1m |
| **身高源** | `des_roll_pitch_height[2]` | `des_roll_pitch_height_motion[2]` |
| **写身高参数安全性** | ❌ 崩 RT 板 | ✅ 安全不崩 |
| **命令级 pos_des[2]** | ❌ 固件不读 | N/A（SetUserGait 读它） |
| **能走+能降身高的步态** | 无（参数崩） | kWalkWave(60) 能走但波浪 |

## 三、全链路盘点（8条通道，穷举完毕）

### 通道 1：303/robot_control_cmd 普通 trot — 命令级
```
set_walk_velocity_step / set_walk_trot_height / set_walk_energy_save
```
- **能走**: ✅ 稳定前进（1.27m @ 0.2m/s）
- **降身高**: ❌ pos_des[2]/value=0x02 固件 LocoGaits 均不读
- **安全**: ✅ 纯命令不写参数

### 通道 2：exec_request — 官方 CyberdogLcm2Cmd
```
set_walk_exec_request_height (7671 LCM, pattern=7→trot)
```
- **能走**: ❌ 狗完全不动
- **降身高**: ❌
- **安全**: ✅ 纯命令

### 通道 3：201 FORCECONTROL — 姿态命令
```
set_body_pose_height / set_body_velocity_height
```
- **能走**: ❌ 201 忽略 vel_des
- **降身高**: ✅ 静态精确（0.235→0.157，pos_des[2] 生效）
- **安全**: ✅

### 通道 4：des_roll_pitch_height 参数 — LocoGaits 身高源
```
set_body_params_lcm (7668 LCM, kind=3 kVEC_X_DOUBLE)
```
- **能走**: ✅ 普通 trot 正常
- **降身高**: △ 路线16 单次写从 0.248 降到 0.228
- **安全**: ❌ **多次写必崩 RT 板**（根因见第三节）
- **崩溃条件**: 走路中写 / 多次写 / 站定写后走久 → RT 板 NaN 倒

### 通道 5：des_roll_pitch_height_motion 参数 — MotionGaits 身高源
```
set_body_params_motion_lcm (7668 LCM, kind=3 kVEC_X_DOUBLE)
```
- **降身高**: ✅ 0.235→0.143（路线22 实测，RT板 ACK）
- **安全**: ✅ 不崩
- **隐患**: 需要配合 MotionGaits 步态才生效

### 通道 6：MotionGaits kTrotInOut(56) — 普通 trot 本体
```
set_walk_trot_in_out (7671 robocmd mode=11 gait=56)
```
- **能走**: ❌ 原地踏步（0.111m/10s）
- **源码分析**: 
  - `OffsetDurationGait` 参数: num_segment=18, offset=(0,9,9,0), duration=(11,11,11,11)
  - 对角同相位（FL+RR、FR+RL），是标准 trot 时序
  - `SetCmd` 速度逻辑正常：`vel_cmd_(0) += 1.0*vel_des[0]` (cmd_source=10>kCyberdogLcmCmd=9)
  - `SetTrotInOutParams` 设置 `gait_type_=&trot_in_out_`，landing offset 用 `trot_in_out_landing_offset`
- **不走根因推测**:
  1. 真机固件(2025-08-01)的 `SetTrotInOutParams` 可能与本地源码(2026-08-08修改版)不同
  2. `trot_in_out_landing_offset: [0.05, 0.06]`（内外八字）影响步态稳定性
  3. FSM 切换已验证正确（gait_id=56≥50→MotionGaits case 2）
  4. 真机可能对 trot_in_out_ 的 offset/duration 参数不响应速度

### 通道 7：自定义 user_gait 步态 — 官方 ProcessUserGaitFile
```
upload_user_gait + set_walk_user_gait_v2 (7671, gait=110)
```
- **能走**: ❌ 芭蕾步态走不动；普通 trot 时序未试
- **降身高**: 理论通过 SetUserGait 的 `pos_cmd_[2]=motion[2]+pos_des[2]`
- **步态格式**: `user_gait_00.toml`（Ballet 芭蕾）每 section 定义 contact=[FL,FR,RL,RR] + duration
- **走不动原因**:
  1. `user_gait_00.toml` 本质是芭蕾（前腿交替后腿），不是行走步态
  2. `NonPeriodicGait::InitGait` 解析 toml → `total_mpc_steps_` = 总步数
  3. `SetUserGait` 设置 `gait_type_` + `body_height_filter_=0.01` + `pos_cmd_[2]=motion[2]+ctrl_cmd_->pos_des[2]`
  4. 芭蕾的 contact 时序不产生前进力（对角腿抬起时间太长）

### 通道 8：kWalkWave(60) — MotionGaits 波浪步态
```
set_walk_wave (7671 robocmd gait=60)
```
- **能走**: ✅ 路线4 验证能前进
- **降身高**: ✅ 路线22 验证 motion[2] 有效 → 可降
- **WalkWave 身高**: `pos_cmd_[2] = motion[2] + walk_wave_height[0] + walk_wave_height[i]`
- **步态性质**: walk_wave_ offset=(18,0,9,27) 36段，波浪形走路（用户不要花哨）

## 四、崩溃根因（写 des_roll_pitch_height 必崩 RT 板）

```
ControlParameter::Set (control_parameters.hpp:312)
  → 对 VecXd 逐个元素循环写（无锁）
  → LocoGaits 500Hz 并发读 des_roll_pitch_height[2]
  → 读到半写完的撕裂值（如 NaN）
  → MPC 解算异常 → 腿指令 NaN → 狗倒
```

**真机固件确认**（libcontrol_rt.so, capstone 反汇编）:
- `ConvexMpcLocoGaits::SetCmd` @ 0x631d8 读 `des_roll_pitch_height` 参数
- `SetDefaultParams`: `pos_cmd_[2] = des_roll_pitch_height[2]`
- 不读 `ctrl_cmd_->pos_des[2]`（与本地源码一致）

**写 motion 参数不崩的原因**: MotionGaits 和 LocoGaits 读不同参数，当前是 LocoGaits 模式时不读 motion 参数 → 无竞争。

## 五、FSM 切换逻辑（kTrotInOut 不走的调试关键）

`fsm_state_locomotion.cpp:342-359`:
```cpp
if (trans_flag_ || push_recovery_flag_) {
    if (gait_id <= kSpecialPronk - 1)  // ≤49
        convex_switch_index_ = 1;  // LocoGaits
    else
        convex_switch_index_ = 2;  // MotionGaits
}
```

- `kSpecialPronk=50`, `kTrotInOut=56≥50` → MotionGaits ✓
- `trans_flag_` 只在 `gait_check_transition_` 为 true 时触发
- LocoGaits 分支(382行)会把 `motion_gaits_->gait_check_transition_` 清 false
- **正确用法**: 从 111 站定直接发 56 → FSM 完整切 MotionGaits
- **错误用法**: 从 303 走路中途切 56 → trans_flag_ 不触发 → 误入 LocoGaits → 56 不被识别 → 不走

## 六、事实定论

| 需求 | 可行性 | 最佳方案 |
|---|---|---|
| 普通trot走路 + 降身高 | ❌ 固件级硬限制 | — |
| 走路（dismiss身高） | ✅ | 303/robocmd trot(3) |
| 静态降身高 | ✅ | 201 pos_des[2]=0.16 (0.157) |
| 走路+降身高（花哨步态） | ✅ | kWalkWave(60) + motion 参数 |
| yaml 固定低姿 | ✅ | 改 RT 板 yaml 重启 (100%安全) |

## 七、附录：有效链路的实际命令

**普通 trot 正常走路**（安全）:
```cpp
motion.set_walk_velocity_step(0.2f, 0, 0, 0.15f); // 303 20Hz
// 或
motion.set_walk_trot_height(0.2f, 0, 0, 0.235f);  // robocmd gait=3
```

**201 静态降身高**（安全，不走路）:
```cpp
motion.set_body_pose_height(0.16f); // 201 + pos_des[2] 20Hz
```

**motion 参数降身高**（安全，需配合能走的 MotionGaits 步态）:
```cpp
motion.set_body_params_motion_lcm(0, 0, 0.16f); // 7668 站定写
```

**kWalkWave 走路**（能走+能降身高，步态波浪）:
```cpp
motion.set_body_params_motion_lcm(0, 0, 0.14f); // 7668 站定写 motion[2]
motion.set_walk_wave(0.2f, 0, 0);               // 7671 robocmd gait=60 20Hz
```
