# cyberdog_race — 项目总览

> **2026 小米杯 CyberDog 2 仿生机器人比赛控制器**
> 本文档由 6 个旧文档合并更新而来（2026-09-02）：
> `方案设计.md` / `架构设计.md` / `开发进度.md` / `开发状态！.md` / `真机降低身高走路-全链路终局总结.md` / `stage4_基准线路.md`
> ⚠ 内容已按 2026-09-02 重构（全编 + launch 参数选路线）更新。

---

## 一、项目概述

- **核心包**：`cyberdog_sim/src/cyberdog_simulator/cyberdog_race/`（ROS2 Galactic + LCM + OpenCV 的 C++ 控制器）
- **开发环境**：
  - 仿真容器镜像 `cyberdog_sim:v2026`（`~/Downloads/cyberdog_race2026.tar`）
  - 启动脚本 `~/Mi/cyberdog_sim/src/cyberdog_simulator/cyberdog_race/tools/build/start_sim.sh`（`--full-build` 全量 / 默认只编 locomotion + race）
  - 真机编译必须在 **NX 板**（VM=x86 / NX=aarch64，架构不同不能交叉用）
- **视觉主传感器**：RGB_camera（巡线/颜色）+ D435（目标识别/测距）+ Lidar（找出口）

---

## 二、整体架构

### 2.1 三层隔离

```
仿      真（stages/virtual/ + vision/virtual/） → Stage1~6
真      机（stages/real/）                     → Stage1Real~6Real（独立类体系，与仿真互不引用）
测      试（test/real/ + test/function/）       → Stage*RealTest + behavior_test（类名+Test后缀）
测试编译开关                                   → cmake -DUSE_TEST_REAL_STAGE*=ON 等
```

- 🚫 真机类不得引用仿真类，反之亦然；测试类不污染正式目录
- 测试通过 cmake 条件编译控制（算法替换实验）；**参数/路线差异走运行期参数**（见 2.3）

### 2.2 编译宏体系（`debug_config.hpp`）

所有平台/功能切换走宏，严禁在 .cpp/.hpp 硬编码：

| 宏 | 作用 | 状态（2026-09-02） |
|---|---|---|
| `REAL_DOG` | 定义=真机代码，注释=仿真代码 | ✅ 真机模式（`/mi_desktop_48_b0_2d_7b_02_c7`） |
| `LLM_MODE_PROXY` / `LLM_MODE_API` | 二选一（有 #error 互斥保护） | ✅ PROXY（ROS2 Service `/llm_ask`） |
| `ENABLE_WEB_STREAMING` | MJPEG 推流（注释=零开销） | ✅ 8080 |
| `DISABLE_D435_SUB` | 关闭 D430i 红外/深度订阅 | ✅（任务只用 RGB） |
| `USE_CENTER_CAM` | 用 center 相机（gc02m1） | ✅ |
| `DEBUG_*` | 调试宏，正式比赛前全注释 | ⚠ 仍有 DEBUG_MOTION/SENSOR/STAGE/END_STAGE 待清 |
| `DEBUG_STAGE4_TEST_ROUTE/2/NO_COMP` | ~~Stage4 路线宏~~ | ❌ **已删除**（2026-09-02 迁到运行期参数） |

**⚠ 真机/仿真话题名**：宏内按 `REAL_DOG` 区分（真机 `/image_center`、`/camera/imu`、`/scan`、`/odom_out` 等）。

### 2.3 运行期版本选择（2026-09-02 重构，核心）

**背景**：Stage4 三套路线的差异主要是距离/补偿/流转（数据 + 少量状态机分支），曾用 `debug_config.hpp` 宏 + `build_bins.sh` 的 sed 改成 17 个二进制 → **技术债重**（sed 易错、touch 强编、md5 查重、宏状态漂移）。

**新方案：一个二进制全编 + launch 参数选实现**（ROS 标准用法，退役 sed/多二进制）：

```
编译期：三个类 Stage4Real / Stage4RealTest / Stage4RealTest2 全编进 race_controller（1 个二进制）
运行期：launch 传参数 → 构造函数 if 选一个类 new 出来 → 全程用它
```

**链路**：`launch` 参数(`stage4_impl:=test2`) → 等价 `ros2 run ... -p stage4_impl:=test2` → `rclcpp::init` 解析 → 参数服务器 → C++ `declare_parameter` / `get_parameter` → `if` 分支 `new` 对应类。

**新增一条路线的模板**：
1. 继承 `Stage4Real`（在 `src/test/real/` 下新建 `stage4_real_testN.hpp/cpp`）
2. override `set_route_params()`（距离/补偿）+ 流转 hook（`route_after_fwd1m` / `route_after_round_out` / `route_after_exit_fwd1`）
3. `race_controller.cpp` 加一个 `stage4_impl` 分支；`CMakeLists.txt` 无条件编入
4. **无需改宏、无需重编其他路线**

### 2.4 线程模型与数据流

```
主线程（100Hz）        视觉线程（30Hz）
  状态机切换      ←→   相机图像处理（检测放锁外）
  运动控制输出           结果写共享变量
  读视觉结果            lane/ball/detect_result
```

- 回调只写锁，锁内 < 0.1ms；OpenCV/JPEG 放锁外
- 读者自行加锁快照；Web 推流纯展示（锁内快照、锁外渲染）
- 颜色空间统一 **BGR**（`on_rgb` 统一 `cv_bridge` 转 BGR）

### 2.5 文件结构（最新）

```
cyberdog_race/
├── CMakeLists.txt / package.xml / README！.md / agent.md
├── launch/race.launch.py          # ★运行期版本选择入口
├── config/camera_hw.yaml          # 调参配置（相机硬件）
├── doc/LLM_GUIDE.md               # LLM 集成指南
├── srv/LLMAsk.srv
├── scripts/                       # ★ ROS py 层（s4_detect_server / sensor_probe 等，先 py 后 cpp）
├── tools/                         # ★ 通用开发工具 + 部署 sh（build/ llm/ vision/ probe/ camera/ record/ rviz/ robot/ scripts/）
├── models/                        # cola.onnx / soccer.onnx
├── include/cyberdog_race/
│   ├── debug_config.hpp           # 宏唯一入口
│   ├── race_controller.hpp / motion_ctrl.hpp / sensor_data.hpp / llm_helper.hpp
│   ├── stages/stage_base.hpp + virtual/ + real/
│   ├── utils/（web_streamer / led_indicator）
│   └── vision/virtual/ + real/
└── src/
    ├── main.cpp / race_controller.cpp / motion_ctrl.cpp / llm_helper.cpp / center_cam.cpp
    ├── stages/virtual/ + real/
    ├── utils/web_streamer.cpp
    ├── vision/virtual/
    └── test/
        ├── function/（behavior_test: 21 个动作/算法测试）
        └── real/（stageN_real_test + stage4_real_test2 + lane_detector_v2）
```

---

## 三、六大赛段设计（源自《方案设计》）

### 第一赛段 石径探路
- 石板路（宽30cm/高5cm/间隔20cm）+ 黄色边界巡线；找到第一个弯道拐弯进入第二段
- **方案**：HSV 提取黄线（H20-35,S>100,V>100）逐行扫描中心线 → PD 控 yaw；弯道单边跟踪；IMU 累计 90° + 双边界再现判断结束
- **真机**：Stage1Real 步高 0.15、前进 6m 巡线 + IMU 90° 转弯

### 第二赛段 荒野寻珠
- 4×4 球阵，入口固定 3 蓝球，其余每行每列各 1 橙球（共 4）；出口左上角
- **方案（已实现）**：S 型路径点导航（通道走，不经过球位）+ 到达后左右扫 ±40° 找橙球，<0.5m 才冲击，撞完退回
- **球距测算经验**：面积等效半径 `r=sqrt(area/π)` 比外接圆稳；焦距从 xacro 精确算 `fx≈402px`；低通 α=0.6；HSV H5-25 S>80 V>80

### 第三赛段 曲道冲锋
- 曲线-直线-曲线 S 形赛道，全程不得踩线；出口在右上角
- **方案（已实现）**：里程计路径点导航为主（KP_IMU=0.8）+ 视觉微调（KP_VIS=0.15）；LaneDetector RELAXED 模式
- 路径点：`(0,5.35)→(2.4,5.8)→(2.4,6.0)→(2.9,6.1)→(2.9,6.7)`；出口 `(2.9,7.35)`

### 第四赛段 深隧寻珍（真机最完善）
- 横向通道 + 3 竖向通道，随机分布可乐瓶/橙球/足球 + 限高杆/方块障碍
- **方案**：固定顺序 C1→C2→C3 通道导航；可乐/足球用 ONNX 模型（cola.onnx/soccer.onnx，opset 降到 13，NX python 推理 640），橙球 CV，限高杆/蓝障碍 CV；TTS 播报；去程末端停点识别
- **真机 Stage4 正式版 = 蛇形三通道巡逻**（详见第六节）

### 第五赛段 孤梁稳渡
- 连续独木桥（直行→左转→方形区），四足全过虚线才可跳下
- **方案**：低速居中 + 黄线巡线（复用 LaneDetector）+ 转弯检测 + 虚线里程计计距 + 跳跃指令
- **真机**：Stage5Real 完整状态机（WALK_SECTION1~5 + TURN + JUMP_PREPARE 等，308 行）

### 第六赛段 撷金建功
- 跳下后在地面区，把足球踢出出口，走到终点圈四足在圈内趴下
- **方案**：Lidar 找出口（空旷方向）+ 相机找球 + 推球修正 + 终点圈趴下
- **真机**：Stage6Real 完整流程（MOVE_FORWARD + 斜退 + 趴下加固，164 行）

---

## 四、赛段进度与实现状态（2026-09-02 更新）

| 赛段 | 仿真 | 真机 | 备注 |
|---|---|---|---|
| Stage1 石径探路 | 🟢 | 🟢 真机实现 | 巡线 + IMU 转弯 |
| Stage2 荒野寻珠 | 🟢 | 🟢 真机实现 | S 型 + 扫描撞球；转向补偿已标定 |
| Stage3 曲道冲锋 | 🟢 | 🟢 真机实现 | 破限低头前进 |
| Stage4 深隧寻珍 | 🟢 | 🟢 **正式/test/test2 三路线** | 蛇形三通道，ONNX+C V |
| Stage5 孤梁稳渡 | 🟢 | 🟢 真机实现 | 独木桥状态机完整 |
| Stage6 撷金建功 | 🟢 | 🟢 真机实现 | 推球 + 趴下 |
| 真机测试类 | — | 🔵 Test2/3 已实现 | 伙伴算法实验（cmake 开关） |

---

## 五、真机底层研究：降低身高走路（全链路终局结论）

### 核心结论
**真机普通 trot 走路时，无法通过外部命令动态降低身体高度。**
- LocoGaits 身高源 = `des_roll_pitch_height[2]` 用户参数
- 写该参数 → RT 板崩溃（`ControlParameter::Set` 无锁循环写 vs 500Hz 读 → 撕裂 → MPC NaN）
- 命令级通道（pos_des[2]/body_height/value=0x02）LocoGaits 固件均不读取

### 两大控制器对比
| | LocoGaits | MotionGaits |
|---|---|---|
| 步态范围 | gait_id ≤ 49（trot=3） | gait_id ≥ 50（56/60/80/110） |
| 走路稳定性 | ✅ 普通 trot 稳走 1.27m | ❌ 56 原地踏步 / 60 波浪 |
| 身高源 | `des_roll_pitch_height[2]` | `des_roll_pitch_height_motion[2]` |
| 写身高参数安全 | ❌ 崩 RT 板 | ✅ 安全 |
| 能走+能降身高 | 无 | kWalkWave(60) 波浪 |

### 可用链路（附录：实际命令）
```cpp
// 普通 trot 正常走路（安全）
motion.set_walk_velocity_step(0.2f, 0, 0, 0.15f);   // 303 20Hz
motion.set_walk_trot_height(0.2f, 0, 0, 0.235f);    // robocmd gait=3

// 201 静态降身高（安全，不走路）
motion.set_body_pose_height(0.16f);                  // 201 + pos_des[2]

// motion 参数降身高（安全，需配合能走的 MotionGaits 步态）
motion.set_body_params_motion_lcm(0, 0, 0.16f);      // 7668 站定写

// kWalkWave 走路（能走+能降身高，步态波浪）
motion.set_body_params_motion_lcm(0, 0, 0.14f);
motion.set_walk_wave(0.2f, 0, 0);                    // 7671 gait=60
```

### 其他真机走路姿态定论
- **roll 走路** ❌ 不可保持（rpy_cmd_scale=[0,1,0] 只启用 pitch；走路时 rpy_des[0] 强制归零）
- **pitch 走路** ✅ `303` 带 `rpy_des[1]`（正值=低头），步态硬限 ±5.7°；破限用 `x_effect_scale_pos=+30` 放大（可到 14°+），用完复位 -0.55
- **yaml_parameter topic 真机是死通道**（NX 无订阅者）；`interface_request`(7668) 是官方参数通道

---

## 六、Stage4 真机基准线路（正式版定稿，2026-08-22）

> 代码：`Stage4Real`（正式）→ `launch stage4_impl:=formal`；test/test2 路线见 `Stage4RealTest` / `Stage4RealTest2`

### 6.1 结构
蛇形三通道巡逻，共 3 轮（去程+回程各一遍）：
```
起步前进 → 右转90°进通道 → 去程(全程低头) → 转180°掉头 → 回程 → 右转90°出通道
第3轮结束：左转90° → 前进2.5m离场 → 左转90°收尾 → 保持站立 DONE
```

### 6.2 关键参数表
| 类别 | 参数 | 正式版值 |
|---|---|---|
| 起步 | 轮1/2/3 | 0.8 / 1.1 / 1.0 m |
| 去程 | 轮1/2/3 | 3.5 / 3.35 / 2.0 m |
| 回程 | 轮1/2/3 | 3.55 / 2.45 / 2.25 m |
| 速度 | 前进/回程 | 0.30 m/s（视觉超时 0.20） |
| 补偿 | 左横向 | 0.023（破限段 0.020） |
| 转向 | 容差 / 收尾下限 / 位置保持限幅 | 0.03 rad / 0.05 / ±0.12 |
| 低头 | pitch / 破限 | 0.25 rad(≈14°) / x_effect_scale_pos=+30 |
| 停点 | 提前量 / 停留 / 识别后前进 | 0.3m / 3s(300帧) / 0.7m |
| 模型 | 可乐 / 足球 | conf≥0.80 / conf≥0.70 |
| 站立 | 开跑 / 结束 | body_height≥0.23 / 持续303静止 |

### 6.3 识别对象与处理
| 对象 | 方式 | 处理 |
|---|---|---|
| 可乐瓶 | ONNX conf≥0.80 | 停点播报 |
| 足球 | ONNX conf≥0.70 | 停点播报 |
| 橙球 | CV（圆+橙） | 停点播报 |
| 限高杆 | CV（红横条，闭运算51×15 面积≥1500） | 走动中播报，直接过 |
| 蓝障碍 | CV（H95-112 S100-200 V150-255） | 走动中播报"不可跨越" |

---

## 七、构建与运行

### 7.1 编译
```bash
# 仿真（默认）
cd /home/cyberdog_sim && source /opt/ros/galactic/setup.bash
colcon build --merge-install --packages-select cyberdog_race

# 测试版（算法替换实验）
colcon build --cmake-args -DUSE_TEST_REAL_STAGE3=ON

# 真机（在 NX 上编译，aarch64）
bash ~/Mi/cyberdog_sim/src/cyberdog_simulator/cyberdog_race/tools/build/build_race.sh   # 自动同步源码 → NX 编译（-j4 降级 -j2/-j1）
```
⚠ **编译注意**：改 `debug_config.hpp` 后 colcon 增量可能不重编 → `find src/cyberdog_race/src -name "*.cpp" -exec touch {} +` 强制重编。

### 7.2 运行（Stage4 三路线，一个二进制）
```bash
# launch 方式（推荐）
ros2 launch cyberdog_race race.launch.py stage4_impl:=formal|test|test2

# 命令行
ros2 run cyberdog_race race_controller --ros-args -p stage4_impl:=test2

# 真机一键（start_race.sh 带相机/推理服务）
STAGE4_IMPL=test2 bash /SDCARD/race_ws/src/cyberdog_race/scripts/start_race.sh

# 完整 1~6 赛段（DEBUG_* 宏全注释 = 从第1段完整跑）
```

### 7.3 LLM
```bash
# PROXY 模式（比赛推荐）：笔记本跑桥接
source install/setup.bash && export LLM_API_KEY="sk-..."
python3 ~/Mi/cyberdog_sim/src/cyberdog_simulator/cyberdog_race/tools/llm/llm_bridge.py
# 狗→ROS2 Service /llm_ask → 笔记本 → 云端
```

### 7.4 退役说明（2026-09-02）
- `build_bins.sh` / `save_bin.sh`（sed 改宏 + 17 个二进制的多版本机制）**已退役**，改由 launch 参数 + 全编取代
- `debug_config.hpp` 的 `DEBUG_STAGE4_TEST_ROUTE/2/NO_COMP` **已删除**

---

## 八、关键经验与坑（真机）

### 8.1 相机 lifecycle — 不激活没图像
```bash
ros2 lifecycle set /stereo_camera configure && ros2 lifecycle set /stereo_camera activate
ros2 lifecycle set /camera/camera configure && ros2 lifecycle set /camera/camera activate
# gc02m1 彩色相机需 camera_service START_IMAGE_PUBLISH（start_race.sh 已处理）
```
- 真机相机定稿：**gc02m1（cam_id=1）低饱和彩色**（D430i 无 RGB、ov13b10 视角差、鱼眼畸变）
- center_cam 需 `taskset -c 2,3` 绑核防 VI 丢帧

### 8.2 LCM 多播路由
- 真机 LCM 走 UDP 多播 `239.255.76.67`；外部 PC 需 `sudo route add -net 239.255.0.0 netmask 255.255.0.0 eth0`
- 端口：`robot_control_cmd`=7671，`interface_request`=7668（参数通道），`global_to_robot` 里程计 7667

### 8.3 RMW/DDS — 订阅全断的坑
- NX 用 CycloneDDS + `ROS_DOMAIN_ID=42` + `CYCLONEDDS_URI=/etc/mi/cyclonedds.xml`
- 必须 source `/etc/mi/ros2_env.conf` 启动（`start_race.sh` 已处理），否则回退 FastDDS 收不到数据

### 8.4 Web 推流四大坑（2026-08-06 踩过）
1. **SIGPIPE 必须忽略**（浏览器切流关闭旧连接 → write SIGPIPE → 进程崩）
2. 客户端线程必须 detach（不能 join + erase）
3. 断连检测用 poll()，否则线程堆积 → 503 黑屏
4. 路由前剥离 query string（`?t=时间戳`），否则 404

### 8.5 运动控制优先级 / 状态机保护
- motion_manager 优先级：App(0) > Audio(1) > Vis(2) > BluTele(3) > Algo(4)；LCM gamepad=BluTele(3)，APP 发指令会覆盖
- 电量 ≤20% Protect（禁剧烈）/ ≤5% LowPower（强制趴下）/ 0% 关机
- `life_count` 必须每帧递增，否则 500ms 超时保护自动趴下（`MotionCtrl::pub_lcm_cmd` 已实现）
- 电机温度 74°C 禁空翻 / 82°C 警告 / 87°C 保护

### 8.6 线程安全红线
- 锁内操作 < 0.1ms；`on_rgb` 锁外做 CV+JPEG，锁内仅写共享数据
- 读者自行加锁快照；`life_count`、`sensor_mutex_` 等已按规范实现

### 8.7 其他真机上机清单
- SSH：有线 `ssh mi@192.168.55.1`（密码 123）/ 无线 APP 内看 IP
- 数据落盘：`/tmp/race_run.log`（start_race.sh tee）、`~/.ros/log/race_controller_*.log`
- NX 本地 python 推理服务：`s4_detect_server.py`（unix socket /tmp/s4_detect.sock，两模型并行 ~2.2s）

---

## 九、代码规范（AI 修改必读）

1. **宏编译体系**：所有平台/功能切换走 `debug_config.hpp`，严禁在 .cpp/.hpp 硬编码 topic 名
2. **三层隔离**：真机/仿真/测试互不引用；测试类不污染正式目录
3. **线程安全**：回调只写锁、锁内 <0.1ms、读者快照
4. **颜色空间统一 BGR**
5. **新增赛段**：继承 `StageBase`（init/run/is_done + 参数虚方法）
6. **新增 Stage4 路线**：继承 `Stage4Real` override `set_route_params` + hooks（见 2.3）
7. 正式比赛前所有 `DEBUG_*` 宏注释掉
