# cyberdog_race 开发状态总览

> 更新日期: 2026-07-08

---

## 一、编译宏总览（`debug_config.hpp`）

| 宏 | 状态 | 说明 |
|---|---|---|
| `DEBUG_VISION` | 🔒 注释 | 弹窗可视化（cv::imshow） |
| `DEBUG_MOTION` | 🔒 注释 | 运动指令日志 |
| `DEBUG_SENSOR` | 🔒 注释 | 传感器数据日志 |
| `DEBUG_STAGE` | 🔒 注释 | 状态机切换终端打印 |
| `DEBUG_SINGLE_STAGE` | 🔒 注释 | 只跑单个赛段 |
| `DEBUG_START_STAGE` | 🔒 注释 | 从指定赛段开始 |
| `DEBUG_END_STAGE` | 🔒 注释 | 跑完指定赛段停止 |
| `DEBUG_NO_HIT` | 🔒 注释 | 禁用撞球 |
| `DEBUG_TEST_BEHAVIOR` | 🔒 注释 | 行为测试模式（1-8号测试） |
| **`ENABLE_WEB_STREAMING`** | ✅ 启用 | **Web 推流总开关（含仪表盘渲染）** |
| `WEB_STREAM_PORT` | ✅ 8080 | 推流端口号 |
| `REAL_DOG` | 🔒 注释 | **仿真/真机模式切换** |
| **`LLM_MODE_PROXY`** | ✅ 启用 | **LLM ROS2 Service 模式（比赛推荐）** |
| `LLM_MODE_API` | 🔒 注释 | LLM libcurl 直连模式（二选一） |
| `LLM_SERVICE_NAME` | `"/llm_ask"` | ROS2 Service 名称 |
| `LLM_TIMEOUT` | 3 (PROXY) / 5 (API) | LLM 调用超时（秒） |

---

## 二、已实现功能 ✅

### 2.1 Web 推流 — 嵌入式 MJPEG HTTP 服务

| 功能 | 文件 | 说明 |
|---|---|---|
| MJPEG HTTP 服务器 | `utils/web_streamer.hpp/cpp` | POSIX socket 实现，零外部依赖 |
| 多客户端支持 | 同上 | 每连接独立线程 detach，`active_clients_` 原子计数 |
| `/stream` — 原始相机 | 同上 | JPEG 质量 70, 30fps |
| `/stream/debug` — 标注画面 | 同上 | 黄线边界、球圈、传感器文字叠加 |
| `/stream/lidar` — 雷达俯视图 | 同上 | 240×240, 5fps, 距离环+红近绿远扫描点 |
| `/stream/track` — 里程轨迹图 | 同上 | 240×240, 5fps, 历史轨迹+当前朝向箭头 |
| `/stream/telemetry` — 遥测仪表盘 | 同上 | 320×240, 5fps, yaw罗盘+身高柱+赛段状态 |
| `/stream/d435` — D435 相机 | 同上 | 640×480, 30fps |
| `/stream/dark` — 降暗画面 | 同上 | 曝光偏移后处理画面 |
| `/settings` — 相机设置页面 | 同上 | 曝光偏移滑块、JPEG 质量滑块 |
| `/api/camera/settings` | 同上 | JSON 读写相机参数，conf 文件持久化 |
| `/api/telemetry` | 同上 | JSON 遥测数据 |
| 交互式 Web 前端 | 同上 | 下拉切换流、展开/折叠面板、FPS 计数器 |
| 编译宏控制 | `debug_config.hpp` | `ENABLE_WEB_STREAMING` 注释=零开销 |

### 2.2 LLM 大模型控制

| 功能 | 文件 | 说明 |
|---|---|---|
| ROS2 Service 定义 | `srv/LLMAsk.srv` | `string prompt --- string response bool success` |
| LLM 助手类 | `llm_helper.hpp/cpp` | 双模式（PROXY / API），宏互斥 |
| PROXY 模式 | 同上 | `Client<LLMAsk>` → `async_send_request` |
| API 模式 | 同上 | libcurl HTTPS POST → DeepSeek/OpenAI |
| LLM 桥接节点 | `tools/llm_bridge.py` | 笔记本端 ROS2 Node，监听 `/llm_ask`，转发云端 |
| LLM 使用指南 | `doc/LLM_GUIDE.md` | API 说明、3种接入方案、注意事项、测试方法 |
| 编译宏互斥检查 | `debug_config.hpp` | `#if defined(A) && defined(B) → #error` |

### 2.3 真机适配框架

| 功能 | 文件 | 说明 |
|---|---|---|
| 仿真/真机双模式 | `debug_config.hpp` | `REAL_DOG` 宏切换 |
| Topic 名宏化 | `debug_config.hpp` | 5 个传感器 topic 用 `TOPIC_*` 宏，两套值 |
| ROS2 `/odom` 订阅 | `race_controller.cpp` | `on_odom()` 真机里程计回调 |
| LCM `state_estimator` 订阅 | `race_controller.cpp` | `on_state_estimator()` 真机状态估计回调 |
| LCM 参数下发框架 | `race_controller.cpp` | `apply_stage_params()` 末尾伪代码占位 |

### 2.4 包结构整理

| 改动 | 说明 |
|---|---|
| `web_streamer` 从 `vision/real/` → `utils/` | 分类纠错（它是通用 HTTP 服务，不是视觉检测器） |
| `stages/real/` + `vision/real/` 加 `README.md` | 标注空骨架为"待开发"，防止混淆 |
| `gamepad_lcmt.hpp` 保留不动 | 被 `motion_ctrl.hpp` 引用，且与 gazebo 版本不同 |

---

## 三、代码质量修复历史

### 本轮会话修复（2026-07-08）

| # | 级别 | 文件 | 问题 | 修复 |
|---|---|---|---|---|
| P1 | 🔴 | `race_controller.cpp` | `on_rgb()` 持锁做 OpenCV 检测+JPEG编码 | 检测移出锁，锁内仅写共享数据（<0.01ms） |
| P2 | 🔴 | `race_controller.cpp` | `control_loop()` 内 120 行渲染代码 | 抽取 `render_track/telemetry/lidar_frame()` 3个方法 |
| P3 | 🔴 | `race_controller.cpp` | `control_loop()` 冗余 snapshot 拷贝+写回 | 删除（赛段通过引用直读最新数据） |
| S1 | 🟡 | `race_controller.cpp` | `odom_history` 记录无锁读 sensor_ 字段 | 加 `lock_guard` 快照 |
| S2 | 🟡 | `motion_ctrl.cpp` | publish-then-reset 依赖 LCM 同步语义 | 添加注释说明 |
| S3 | 🟡 | `race_controller.cpp` | `vision_result` 跨线程无锁访问 | 添加注释说明（非关键路径） |
| S4 | 🟡 | `.hpp` + `.cpp` | `read_sensor_snapshot()` 死代码 | 删除声明+定义 |
| S5 | 🟡 | `llm_helper.cpp` | `snprintf` 无截断检测 | 加返回值检查，溢出打日志 |
| S6 | 🟡 | `race_controller.cpp` | 渲染方法无锁读 sensor_ 字段 | 每个渲染方法开头加锁快照 |
| N1 | 🟡 | `sensor_data.hpp` | `lidar_ranges` 死数据（拷贝但从不读取） | 删除字段 + `on_lidar()` 中的赋值 |
| N2 | 🟡 | `sensor_data.hpp` | 缺少 `#include <vector>` | 添加 |

### 之前会话修复

| # | 文件 | 问题 | 修复 |
|---|---|---|---|
| P0 | `web_streamer.cpp` | `stop()` 阻塞在 `accept()` | `shutdown(server_fd_, SHUT_RDWR)` 强制唤醒 |
| P1b | `web_streamer.cpp` | 客户端线程僵尸积累 | `active_clients_` 原子计数，三路退出均递减 |
| P2 | `motion_ctrl.cpp` | `set_step_height` 用错 LCM 端口 | 改为 `ctrl_lcm_.publish()` (端口 7671) |
| P4 | `web_streamer.cpp` | `send_header` 格式化溢出 | `snprintf` 返回值校验再 write |
| P7 | `web_streamer.cpp` | `read_line` 慢速攻击 | 加 4096 字节上限 |
| — | `race_controller.cpp` | `on_d435` unused param 警告 | `(void)msg` + `#ifdef` 守卫 |
| — | `race_controller.cpp` | narrowing conversion 警告 | `cv::Scalar(double,double,double)` |
| — | `CMakeLists.txt` | `state_estimator_lcmt.hpp` 找不到 | 加 `cyberdog_visual/lcm_type` include 路径 |

---

## 四、未实现 / 待完成 ⏳

### 4.1 LLM 集成

| 项目 | 状态 | 说明 |
|---|---|---|
| LLM 接入赛段决策 | 🔴 未做 | `llm_helper` 已联通但 `ask()` 从未被任何赛段调用 |
| LLM 异步调用（非阻塞） | 🔴 未做 | 当前 `ask()` 同步阻塞，需包装为异步回调 |

### 4.2 Web 前端

| 项目 | 状态 | 说明 |
|---|---|---|
| 顶部状态栏实时数据 | 🔴 空壳 | `/api/telemetry` 返回占位 JSON |
| D435 深度图伪彩色 | 🔴 未做 | 当前只推流 color 原图 |
| 四足接触状态显示 | 🔴 未做 | `leg_control_data_lcmt` 可读 |
| 命令发送按钮 | 🔴 未做 | 需 HTTP POST → C++ 安全链路 |

### 4.3 真机适配

| 项目 | 状态 | 确认方式 |
|---|---|---|
| 5 个传感器 topic 精确名称 | 🟡 宏已占位 | `ros2 topic list` 后替换 TODO 字符串 |
| 图像编码格式 (bgr8 vs rgb8) | 🔴 待确认 | `ros2 topic echo` 看 encoding |
| `use_rc=0` 是否生效 | 🔴 待确认 | 真机发 LCM 后看反应 |
| 参数下发通道 | 🔴 待确认 | ROS2 `yaml_parameter` vs LCM `control_parameter_lcmt` |
| `body_height` 来源 | 🟡 假设置 | 真机对比 `state_estimator.p[2]` 与实际身高 |

### 4.4 赛段逻辑

| 赛段 | 状态 | 备注 |
|---|---|---|
| Stage1 石径探路 | 🟢 可运行 | 视觉/IMU融合巡线 + 路口转向 |
| Stage2 荒野寻珠 | 🟢 可运行 | S型扫描 + 撞球 |
| Stage3 曲道冲锋 | 🟡 待调参 | 路径点导航 + 视觉微调 |
| Stage4 深隧寻珍 | 🔴 框架 | 30+状态状态机，视觉已接但逻辑未完成 |
| Stage5 孤梁稳渡 | 🔴 框架 | 独木桥居中 |
| Stage6 撷金建功 | 🔴 框架 | YOLO + LiDAR（视觉模块占位） |

### 4.5 视觉算法

| 模块 | 状态 | 说明 |
|---|---|---|
| LaneDetector (HSV 巡线) | 🟢 完成 | STRICT/RELAXED 双模式 |
| BallDetector (HSV 球检测) | 🟢 完成 | 橙/蓝/白三色 |
| Stage4Detector (Hough 圆) | 🟡 框架 | 5 种目标检测，未对接状态机 |
| YOLO 目标检测 | 🔴 占位 | `vision/real/yolo_detector.hpp` 只有 TODO |
| LiDAR 定位 | 🔴 占位 | `vision/real/lidar_locator.hpp` 只有 TODO |
| 真机赛段 | 🔴 占位 | `stages/real/` 3个头文件，0个实现 |

---

## 五、文件清单

### 新增文件

| 文件 | 说明 |
|---|---|
| `include/cyberdog_race/utils/web_streamer.hpp` | WebStreamer 类声明（7帧缓冲 + 相机设置） |
| `src/utils/web_streamer.cpp` | WebStreamer 完整实现（670行） |
| `include/cyberdog_race/llm_helper.hpp` | LLM 助手类声明（双模式） |
| `src/llm_helper.cpp` | LLM 实现（PROXY → ROS2 Service, API → libcurl） |
| `srv/LLMAsk.srv` | 自定义 ROS2 Service 定义 |
| `doc/LLM_GUIDE.md` | LLM 使用指南 |
| `tools/llm_bridge.py` | 笔记本端 ROS2 桥接节点 |
| `include/cyberdog_race/vision/real/yolo_detector.hpp` | YOLO 占位 |
| `include/cyberdog_race/vision/real/lidar_locator.hpp` | LiDAR 定位占位 |
| `include/cyberdog_race/stages/real/stage4~6_real.hpp` | 真机赛段占位 |
| `include/cyberdog_race/stages/real/README.md` | 真机赛段说明 |
| `include/cyberdog_race/vision/real/README.md` | 真机视觉说明 |

### 修改文件

| 文件 | 本轮会话改动要点 |
|---|---|
| `debug_config.hpp` | LLM 宏（PROXY/API 互斥 + Service名 + 超时） |
| `CMakeLists.txt` | `rosidl_generate_interfaces` + `rosidl_target_interfaces` + libcurl |
| `package.xml` | `rosidl_default_generators/runtime` + `member_of_group` |
| `race_controller.hpp` | LLM 成员、渲染方法声明、删除 `read_sensor_snapshot` |
| `race_controller.cpp` | P1-P3 修复、渲染抽取、锁快照、LLM init(this) |
| `sensor_data.hpp` | 加 `#include <vector>`、删除死数据 `lidar_ranges` |
| `motion_ctrl.cpp` | LCM 同步注释 |
| `llm_helper.hpp` | `init(rclcpp::Node*)` 签名、`rclcpp` 无条件包含 |
| `llm_helper.cpp` | PROXY用 ROS2 Service、API加 snprintf 溢出检查 |
| `tools/llm_bridge.py` | Flask → ROS2 Node 重写 |

---

## 六、API 路由总表

| 路径 | MIME | 帧率 | 说明 |
|---|---|---|---|
| `/` | `text/html` | — | 交互式调试面板 |
| `/stream` | `multipart/x-mixed-replace` | 30Hz | 原始 RGB 相机 |
| `/stream/debug` | 同上 | 30Hz | 标注画面 |
| `/stream/lidar` | 同上 | 5Hz | LiDAR 俯视雷达图 |
| `/stream/track` | 同上 | 5Hz | 里程轨迹图 |
| `/stream/telemetry` | 同上 | 5Hz | 遥测仪表盘 |
| `/stream/d435` | 同上 | 30Hz | D435 深度相机 |
| `/stream/dark` | 同上 | 30Hz | 降暗后处理画面 |
| `/settings` | `text/html` | — | 相机设置页面 |
| `/api/camera/settings` | `application/json` | — | 相机参数读写 |
| `/api/telemetry` | `application/json` | 轮询 | 遥测数据 |

---

## 七、编译命令

```bash
# 进入 Docker
docker exec -it cyberdog bash

# 仿真模式（默认：ENABLE_WEB_STREAMING + LLM_MODE_PROXY）
cd /home/cyberdog_sim
source /opt/ros/galactic/setup.bash
colcon build --merge-install --packages-select cyberdog_race

# 真机模式（先取消 #define REAL_DOG 的注释）
colcon build --merge-install --packages-select cyberdog_race

# 无 LLM 模式（注释 LLM_MODE_PROXY）
colcon build --merge-install --packages-select cyberdog_race

# LLM API 直连模式（切换为 LLM_MODE_API）
colcon build --merge-install --packages-select cyberdog_race

# 启动笔记本端 LLM 桥接
source install/setup.bash
export LLM_API_KEY="sk-your-key-here"
python3 tools/llm_bridge.py
```

---

## 八、真机 SSH 检查清单

```bash
# 1. 记录所有 ROS2 topic
ros2 topic list > topics.txt

# 2. 查看每个 topic 的消息类型
ros2 topic info /camera/color/image_raw
ros2 topic info /imu
ros2 topic info /scan
ros2 topic info /odom

# 3. 查看图像编码
ros2 topic echo /camera/color/image_raw --once | grep encoding

# 4. 检查 LCM 通道
lcm-spy  # 观察有哪些活跃通道

# 5. 测试 LLM 通信链路
ros2 service call /llm_ask cyberdog_race/srv/LLMAsk "{prompt: 'hello'}"
```

---

## 九、当前代码量统计

| 文件 | 行数 | 说明 |
|---|---|---|
| `race_controller.cpp` | 636 | 主控制器（含3个渲染方法） |
| `web_streamer.cpp` | 670 | HTTP MJPEG 服务器 |
| `llm_helper.cpp` | 124 | LLM 双模式实现 |
| `motion_ctrl.cpp` | 81 | LCM 运动控制 |
| `behavior_test.cpp` | ~200 | 8种行为测试 |
| 6个赛段 .cpp | ~900 合计 | Stage1~6 虚拟赛段 |
| 3个检测器 .cpp | ~400 合计 | Lane/Ball/Stage4 |
| **总计** | **~3000** | |
