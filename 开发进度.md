# 开发进度

## 环境

- 仿真容器镜像：`cyberdog_sim:v2026`，来源 `~/Downloads/cyberdog_race2026.tar`
- 启动脚本：`./start_sim.sh --full-build`（全量编译）或 `./start_sim.sh`（只编 cyberdog_locomotion + cyberdog_race）
- 宿主机 `src/` 挂载进容器，代码修改实时同步，但需重新编译才生效

### 常用命令
```bash
# 启动仿真（全量编译）
./start_sim.sh --full-build

# 只编 race 包（改了 stage/motion_ctrl 等）
docker exec -it cyberdog bash -c "cd /home/cyberdog_sim && source /opt/ros/galactic/setup.bash && colcon build --merge-install --packages-select cyberdog_race"

# 编 locomotion 包（改了步高/步态/command_interface 等）
docker exec -it cyberdog bash -c "cd /home/cyberdog_sim && source /opt/ros/galactic/setup.bash && touch src/cyberdog_locomotion/control/src/convex_mpc/convex_mpc_loco_gaits.cpp && colcon build --merge-install --packages-select cyberdog_locomotion"

# 启动比赛控制器
docker exec -it cyberdog bash -c "cd /home/cyberdog_sim && source /opt/ros/galactic/setup.bash && source install/setup.bash && ros2 run cyberdog_race race_controller"
```

### 重要：编译注意事项
- `cyberdog_locomotion` 包含两个关键 so：
  - `libcommon.so`：包含 `command_interface.cpp`（步高命令、gamepad 解析）
  - `libcontrol.so`：包含 `convex_mpc_loco_gaits.cpp`（步高上限、步态参数）
- **`libcontrol.so` 默认不会被增量编译触发**，必须用 `touch` 强制重编
- 编译完必须重启 Control 节点才能加载新 so

---

## 赛段进度

| 赛段 | 状态 | 备注 |
|------|------|------|
| 第一赛段 石径探路 | 🟢 可运行 | 视觉/IMU融合巡线，卡住检测，路口转向 |
| 第二赛段 荒野寻珠 | 🟢 可运行 | S型路径点导航，扫描识别橙球，撞球退回 |
| 第三赛段 曲道冲锋 | 🟡 可运行，待调参 | 路径点导航+视觉微调，出口坐标已标定 |
| 第四赛段 深隧寻珍 | 🔴 空实现 | 依赖 YOLO，待集成 |
| 第五赛段 孤梁稳渡 | 🔴 框架搭好 | 虚线检测、跳跃指令未实现 |
| 第六赛段 撷金建功 | 🔴 框架搭好 | Lidar 找出口、推球逻辑待完善 |

---

## 已完成内容

### 基础框架
- 状态机主控节点 `race_controller`，100Hz 控制循环
- 6 个赛段类（Stage1~6），统一 `StageBase` 接口
- 调试宏体系 `debug_config.hpp`（正式比赛前全部注释掉）

### 步高问题（重要经验）
- 步高控制链路：`Gamepad2Cmd` → `step_height_cmd_` → `WrapRange(min, max)` → `SetHeight()`
- `libcontrol.so` 是 2024 年旧版本，必须用 `touch` 强制重编才能更新
- 最终有效改动：
  - `command_interface.cpp` `Gamepad2Cmd`：`step_height[0] = 0.25`
  - `convex_mpc_loco_gaits.cpp` `SetDefaultParams`：`step_height_min_ = 0.06`（防止贝塞尔轨迹起止段贴地被绊）
  - `cyberdog-ctrl-user-parameters.yaml`：`step_height_max = 0.25`

### 转向符号约定（重要）
- `set_velocity(x, y, yaw)` 中：**yaw 正值 = 左转（yaw 增大），负值 = 右转（yaw 减小）**
- `yaw_err = target_yaw - current_yaw`，`yaw_err > 0` 需左转，发正值
- 之前代码符号写反，已全部修正（stage1 转向、stage2 navigate_to/turn_to/scan）

### MotionCtrl
- 只用 `gamepad_lcmt` 通道，`use_rc=0`
- `set_velocity` 每次发完立刻清零摇杆，依赖 100Hz 控制循环持续发命令
- `stop()` 发零速 locomotion 命令
- Ctrl+C 后 `rclcpp::spin` 退出，main 里发 `stand()` 再 `lie_down()` 停止

### 第一赛段
- 黄线巡线：HSV 分割 + 逐行扫描 + 视觉/IMU 加权融合
  - 直道：IMU 回正为主（权重 0.5~0.8），视觉微调
  - 弯道：纯视觉 PD
  - 低通滤波（α=0.6）减少抖动振荡
- 单边检测时 `lane_both_sides=false`，视觉权重降低到 0.2
- 卡住检测：连续 30 帧位移 < 1cm 触发，加速冲过石板（0.5m/s，持续 40 帧）
- 路口触发：里程计 x ∈ [2.95, 3.2] 触发，转向到 yaw = π/2
- 低头：`set_pitch(-0.26f)`（约 15 度）

### 第二赛段
- S 型路径导航，路径点坐标已实测标定
- 到达路径点后左右扫描 ±40° 找橙色球
- 发现球后冲击，退回路径点继续

---

## 关键参数

| 参数 | 当前值 | 说明 |
|------|--------|------|
| 黄线 HSV | H:15-35, S>80, V>80 | 可根据光照调整 |
| KP_YAW | 0.6 | 巡线偏差增益 |
| KD_YAW | 0.08 | 巡线微分增益 |
| stage1 正常速度 | 0.5 m/s | |
| stage1 弯道速度 | 0.25 m/s | |
| stage2 移动速度 | 0.5 m/s | |
| 步高命令值 | 0.25 m | Gamepad2Cmd 硬编码 |
| 步高上限 | 0.25 m | yaml step_height_max |
| 步高下限 | 0.06 m | SetDefaultParams step_height_min_ |
| 路口触发 x 范围 | [2.95, 3.2] | 仿真实测 |
| 转向目标 yaw | π/2 ≈ 1.57 | 进入第二赛段方向 |
| 低头角度 | -0.26 rad | 约 15 度 |

---

## 第二次调试经验（Apr 18）

### 环境问题
- 删除 `build/install` 后全量重编，`libsimbridge.so` 不会被安装到 `install/lib/`，只在 `build/` 里
- `cyberdog_control_launch.py` 启动时需要设置 `LD_LIBRARY_PATH` 包含 `build/` 下的 so 路径，否则报 `cannot open shared object file`
- 已在 `cyberdog_control_launch.py` 里加入 `LD_LIBRARY_PATH` 设置，修改后需重编 `cyberdog_gazebo` 才能更新 install 里的 launch 文件

### 球距离测算（重要经验）
- 原始算法用外接圆半径，近距离时球边缘 HSV 漏检导致半径偏小，距离偏大
- 改进：用面积等效半径 `r = sqrt(area/π)`，比外接圆稳定；取 `max(r_area, r_enc*0.85)` 兼顾近远距离
- 相机焦距从 xacro 精确计算：`fx = 320/tan(0.6981) ≈ 402px`，原来用 600 误差 50%
- 加低通滤波（α=0.6）减少帧间抖动
- 放宽 HSV 范围：`H:5-25, S>80, V>80`（原来 S>120 导致近距离漏检）
- **结论：距离算法调通后精度可用，近距离误差约 10%**

### stage2 扫描识别逻辑（重要经验）
- 原来 `ball_in_arena()` 用 `ball_dist` 估算世界坐标判断是否在球阵范围，但扫描时 yaw 偏转导致坐标估算错误，球明明在范围内却返回 false → 已去掉此限制
- `SCAN_LEFT_CHECK` 原来是一帧判断，没找到立刻切走 → 改为等待 200 帧（2秒），期间任意一帧看到球就确认
- 转动过程中也持续检测，转到位时如果已经看到球直接确认，不需要再停下等待
- `HIT_BALL` 里用 `static` 局部变量记录起始位置有 bug（第二次撞球不重置）→ 改为成员变量 `hit_started_`
- 撞球逻辑：前进 0.3m 后退回路径点，不依赖 `ball_dist` 判断是否撞到

### debug_config.hpp 使用规范
- `DEBUG_NO_HIT`：禁用撞球，只观察视觉，调试距离算法时开启
- `DEBUG_START_STAGE N`：从第 N 赛段开始，调试单个赛段时用
- `DEBUG_END_STAGE N`：跑完第 N 赛段停止
- 正式比赛前所有 `#define DEBUG_*` 全部注释掉

### 关键参数更新

| 参数 | 当前值 | 说明 |
|------|--------|------|
| stage1 正常速度 | 0.45 m/s | 降速减少石板绊脚 |
| stage2 移动速度 | 0.8 m/s | |
| stage2 扫描等待 | 200 帧（2秒）| 停下识别最长等待时间 |
| stage2 撞球距离 | 0.3 m | 前进距离 |
| BALL_DIST_THRESH | 0.75 m | 扫描时认为球够近的阈值 |
| 球焦距 | 402 px | 从 xacro fov 精确计算 |
| 橙色 HSV | H:5-25, S>80, V>80 | 放宽后近距离检测更稳定 |

---

## 第三次调试经验（Apr 20）

### stage3 曲道冲锋

**问题**：纯视觉 PD 控制在 S 形弯道处频繁失效，`filter_continuity` 过滤掉弯道处的黄线点导致 `lane_valid=false`，狗停住或振荡。

**解决方案：改用里程计路径点导航 + 视觉微调**
- 主控：`KP_IMU * yaw_err`（路径点方向误差）
- 辅助：`KP_VIS * lane_offset`（视觉微调，权重 0.15）
- 路径点：`(0,5.35)→(2.4,5.8)→(2.4,6.0)→(2.9,6.1)→(2.9,6.7)`
- 走完路径点后转向 y 正方向，走到 `(2.9,7.35)` 后转向 x 负方向结束

**LaneDetector 模式切换**
- 加入 `STRICT/RELAXED` 两种模式
- stage1/2 用 STRICT（`CONTINUITY_THRESH=0.30`）
- stage3 用 RELAXED（`CONTINUITY_THRESH=0.55`，`LATERAL_THRESH=50px`）
- main.cpp 在赛段切换时自动设置对应模式

### 代码整理（Apr 20）

**架构改进：**
- `stage_base.hpp` 加 `norm_yaw()` 工具函数，消除各 stage 重复的 yaw 归一化代码
- `SensorData` 清理冗余字段：删除 `lane_yaw`、`ball_y`、`lidar_left`、`lidar_right`
- stage1 常量移到 hpp，风格与 stage2/3 统一
- stage2 `DONE` 状态的 `static` 局部变量改为成员变量，修复重新 init 不重置的 bug
- stage1 删除未使用的 `in_turn_`、`run_frames_`、`yaw_start_` 成员变量

### 关键参数更新

| 参数 | 当前值 | 说明 |
|------|--------|------|
| stage1 正常速度 | 0.45 m/s | |
| stage1 弯道速度 | 0.2 m/s | |
| stage2 移动速度 | 0.8 m/s | |
| stage2 扫描转向速度 | 0.2 rad/s | 慢转方便识别 |
| stage2 路径点转向速度 | 0.5 rad/s | |
| stage2 BALL_DIST_THRESH | 0.90 m | 扫描识别距离阈值 |
| stage3 速度 | 0.2 m/s | 路径点导航 |
| stage3 KP_IMU | 0.8 | 路径点方向增益 |
| stage3 KP_VIS | 0.15 | 视觉微调权重 |
| stage3 路径点 | 见代码 | 5个点 |
| stage3 出口坐标 | (2.9, 7.35) | 实测 |
