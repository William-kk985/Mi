# cyberdog_race 开发状态总览

> 更新日期: 2026-07-12

---

## 一、编译宏总览（`debug_config.hpp`）

| 宏 | 状态 | 说明 |
|---|---|---|
| `DEBUG_VISION` | 🔒 注释 | 弹窗可视化（cv::imshow） |
| `DEBUG_MOTION` | 🔒 注释 | 运动指令日志 |
| `DEBUG_SENSOR` | 🔒 注释 | 传感器数据日志 |
| `DEBUG_STAGE` | 🔒 注释 | 状态机切换终端打印 |
| `DEBUG_SINGLE_STAGE` | 🔒 注释 | 只跑单个赛段 |
| `DEBUG_START_STAGE` | 🔒 注释 | 从指定赛段开始 |
| `DEBUG_END_STAGE` | 🔒 注释 | 跑完指定赛段停止 |
| `DEBUG_NO_HIT` | 🔒 注释 | 禁用撞球 |
| `DEBUG_TEST_BEHAVIOR` | 🔒 注释 | 行为测试模式（1-8号测试） |
| **`ENABLE_WEB_STREAMING`** | ✅ 启用 | **Web 推流总开关（含仪表盘渲染）** |
| `WEB_STREAM_PORT` | ✅ 8080 | 推流端口号 |
| `REAL_DOG` | 🔒 注释 | **仿真/真机模式切换** |
| **`LLM_MODE_PROXY`** | ✅ 启用 | **LLM ROS2 Service 模式（比赛推荐）** |
| `LLM_MODE_API` | 🔒 注释 | LLM libcurl 直连模式（二选一） |
| `LLM_SERVICE_NAME` | `"/llm_ask"` | ROS2 Service 名称 |
| `LLM_TIMEOUT` | 3 (PROXY) / 5 (API) | LLM 调用超时（秒） |

### 1.1 仿真话题名

| 宏 | 值 |
|---|---|
| `TOPIC_RGB_CAMERA` | `/RGB_camera/image_raw` |
| `TOPIC_IMU` | `/imu` |
| `TOPIC_LIDAR` | `/scan` |
| `TOPIC_D435` | `/D435/color/image_raw` |

### 1.2 真机话题名（`#define REAL_DOG` 后生效，依据官方 developer_guide）

> CyberDog 2 共 **2 个物理相机模组** + 1 个对齐节点，总计 7 个图像 topic：
> - 前置 AI 相机(`/stereo_camera`)：`/image_rgb`(RGB) `/image_left`(左鱼眼) `/image_right`(右鱼眼)
> - 底部 D430i(`/camera/camera`)：`/camera/infra1`|`infra2`(红外) `/camera/depth`(深度) `/camera/imu`
> - 对齐节点(`/camera/camera_align`)：`/camera/aligned_depth_to_extcolor/image_raw`(对齐深度)
> ⚠ D430i **无 RGB 彩色输出**，只有红外+深度！

| 宏 | 值 | 来源 |
|---|---|---|
| `TOPIC_RGB_CAMERA` | `/image_rgb` | AI 相机 RGB（需 lifecycle 激活 /stereo_camera） |
| `TOPIC_FISH_EYE_LEFT` | `/image_left` | 左鱼眼 灰度（需 lifecycle 激活 /stereo_camera） |
| `TOPIC_FISH_EYE_RIGHT` | `/image_right` | 右鱼眼 灰度（需 lifecycle 激活 /stereo_camera） |
| `TOPIC_IMU` | `/imu` | 身体 IMU |
| `TOPIC_LIDAR` | `/scan` | ⚠ 可能为 `ScanMsg` 非标准 `LaserScan` |
| `TOPIC_D435_INFRA1` | `/camera/infra1/image_rect_raw` | D430i 左目红外（需 lifecycle 激活 /camera/camera） |
| `TOPIC_D435_DEPTH` | `/camera/depth/image_rect_raw` | D430i 深度图 mono16/mm |
| ~~`TOPIC_D435`~~ | ~~`/camera/aligned_depth_to_extcolor/image_raw`~~ | ❌ 已废弃：深度图非彩色，BGR 解码会崩溃 |
| `TOPIC_BMS` | `bms_status` | 电池（`protocol::msg::BmsStatus`） |
| `TOPIC_TOUCH` | `touch_status` | 触摸（`protocol::msg::TouchStatus`） |
| `LCM_ODOM_CHANNEL` | `global_to_robot` | 里程计（`localization_lcmt`, 50Hz, 7667） |
| `LCM_STATE_ESTIMATOR` | `state_estimator` | 状态估计 |
| `LCM_CMD_EXEC` | `exec_request` | 参数下发 |

---

## 二、已实现功能 ✅

### 2.1 Web 推流 — 嵌入式 MJPEG HTTP 服务

| 功能 | 说明 |
|---|---|
| 9 路 MJPEG 流 + 2 个 API + 2 个 HTML 页面 | `/` `/stream` `/stream/debug` `/stream/lidar` `/stream/track` `/stream/telemetry` `/stream/d435` `/stream/dark` `/stream/depth` `/stream/fisheye_left` `/stream/fisheye_right` `/settings` `/api/camera/settings` `/api/telemetry` |
| 多客户端（`active_clients_` 原子计数） | 每连接独立线程 detach |
| 相机设置持久化 | 曝光偏移 + JPEG 质量滑块，conf 文件读写 |
| 交互式 Web 前端 | 下拉切换流 + 展开/折叠面板 + FPS 计数器 |
| 编译宏控制 | `ENABLE_WEB_STREAMING` 注释=零开销 |

### 2.2 LLM 大模型控制

| 功能 | 说明 |
|---|---|
| ROS2 Service 定义 | `srv/LLMAsk.srv` |
| PROXY 模式（比赛推荐） | `Client<LLMAsk>` → `async_send_request` → 笔记本 `llm_bridge.py` → 云端 |
| API 模式（备选） | libcurl HTTPS POST → DeepSeek/OpenAI 直连 |
| 编译宏互斥检查 | `#if defined(A) && defined(B) → #error` |
| LLM 使用指南 | `doc/LLM_GUIDE.md` |

### 2.3 真机适配（对照官方文档全部修正）

| 功能 | 说明 |
|---|---|
| 里程计 | ✅ LCM `global_to_robot`（`localization_lcmt`, 50Hz, 7667），不再用 ROS2 `/odom` |
| 相机 topic | ✅ 6 个 topic 全部依据官方文档修正（`/image_rgb` 等） |
| cv_bridge 编码 | ✅ `on_rgb` 统一转 BGR（cv_bridge 自动处理源编码），三个检测器无需改动 |
| BMS 电池保护 | ✅ 订阅 `bms_status`，soc<20% 自动停止+趴下，soc<30% 每10s警告 |
| 参数下发 | ✅ ROS2 `/yaml_parameter` + LCM 备选通道占位 |
| body_height | ✅ 双来源：`localization_lcmt.xyz[2]` + `state_estimator.p[2]` 覆盖 |
| touch 紧急停止 | 🟡 已注释备用（等 `protocol::msg::TouchStatus` 类型） |

### 2.4 代码质量 — 本轮修复总计 14 项

| # | 级别 | 问题 | 修复 |
|---|---|---|---|
| P1 | 🔴 | `on_rgb()` 持锁做 OpenCV+JPEG（~50ms） | 检测移出锁，锁内仅写共享数据（<0.01ms） |
| P2 | 🔴 | `control_loop()` 内 120 行渲染代码 | 抽取 `render_track/telemetry/lidar_frame()` |
| P3 | 🔴 | 冗余 sensor snapshot 拷贝+写回 | 删除 |
| S1-S6 | 🟡 | 线程安全：odom裸读/渲染裸读/LCM注释/死代码/snprintf | 全部加锁快照或注释 |
| N1-N2 | 🟡 | `lidar_ranges` 死数据 + 缺 `#include <vector>` | 删除+添加 |
| E1 | 🔴 | 三个检测器硬编码 BGR2HSV，真机 RGB 输入会检测错误 | `on_rgb` 统一转 BGR 传入 |
| E2 | 🔴 | 真机里程计走 ROS2 `/odom`（真狗无此 topic） | 改为 LCM `global_to_robot` |
| E3 | 🔴 | touch 订阅用错类型（Int32 接 TouchStatus）会崩溃 | 注释+TODO，等 `bridges` 包 |

### 2.5 包结构整理

| 改动 | 说明 |
|---|---|
| `web_streamer` `vision/real/` → `utils/` | 分类纠错 |
| `stages/real/` + `vision/real/` 加 `README.md` | 标注空骨架 |
| `LocoMode` 枚举常量 | 替代魔法数字 16/22 |
| 新增 `utils/led_indicator.hpp` | LED 赛段指示框架（等 `LedExecute` 类型启用） |
| 新增 `README！.md` | 架构规范文档（含8条AI约束、文件结构、传感器总表） |
| `stages/real/` 补齐全部6个头文件+6个源文件 | `Stage1Real~6Real` 独立骨架，CMakeLists注释占位 |

---

## 三、未实现 / 待完成 ⏳

### 3.1 等真机 SDK（bridges 包）拿到后启用

| 功能 | 当前状态 | 启用方式 |
|---|---|---|
| touch 紧急停止 | 代码已写，类型注释 | 换 `protocol::msg::TouchStatus`，取消注释 |
| BMS 精确字段 | `Float32MultiArray` 占位 | 换 `protocol::msg::BmsStatus` |
| LED 赛段指示 | `led_indicator.hpp` 框架 | 换 `protocol::srv::LedExecute` |
| TOF 独木桥检测 | 订阅+回调已写 | 换 `protocol::msg::HeadTofPayload` 等 |
| 电机温度监控 | `motion_ctrl.hpp` 框架 | 换 `danger_states_lcmt.hpp` |
| cmd 响应确认 | `motion_ctrl.hpp` 框架 | 换 `robot_control_response_lcmt.hpp` |

### 3.2 上真机 SSH 确认后改

| 项目 | 操作 |
|---|---|
| 相机 topic 名 | `ros2 topic list \| grep -E 'image\|camera'` 宏值 |
| LiDAR 类型 | `ros2 topic info /scan` 看是否 `LaserScan` |
| 相机 lifecycle | `ros2 lifecycle set /stereo_camera activate` 等 |
| 参数下发通道 | `lcm-spy` 看 `control_parameter` 是否存在 |

### 3.3 赛段逻辑

| Stage1-2 | 🟢 可运行 | Stage3 | 🟡 待调参 |
| Stage4-6 | 🔴 框架 | Stage1Real~6Real | 🟤 骨架就绪（待实现） |


## 四、API 路由总表

| 路径 | MIME | 帧率 |
|---|---|---|
| `/` | `text/html` | — |
| `/stream` | MJPEG | 30Hz |
| `/stream/debug` | MJPEG | 30Hz |
| `/stream/lidar` | MJPEG | 5Hz |
| `/stream/track` | MJPEG | 5Hz |
| `/stream/telemetry` | MJPEG | 5Hz |
| `/stream/d435` | MJPEG | 30Hz |
| `/stream/dark` | MJPEG | 30Hz |
| `/stream/depth` | MJPEG | 30Hz |
| `/stream/fisheye_left` | MJPEG | 30Hz |
| `/stream/fisheye_right` | MJPEG | 30Hz |
| `/settings` | `text/html` | — |
| `/api/camera/settings` | JSON | — |
| `/api/telemetry` | JSON | 轮询 |

---

## 五、编译命令

```bash
# 仿真（默认）
cd /home/cyberdog_sim && source /opt/ros/galactic/setup.bash
colcon build --merge-install --packages-select cyberdog_race

# 真机：取消 #define REAL_DOG 的注释后同上

# 笔记本 LLM 桥接
source install/setup.bash
export LLM_API_KEY="sk-your-key-here"
python3 tools/llm_bridge.py
```

---

## 六、真机 SSH 检查清单

```bash
# 1. 激活相机（否则没有 /image_rgb 等 topic）
ros2 lifecycle set /stereo_camera configure && ros2 lifecycle set /stereo_camera activate
ros2 lifecycle set /camera/camera configure && ros2 lifecycle set /camera/camera activate
ros2 lifecycle set /camera/camera_align configure && ros2 lifecycle set /camera/camera_align activate

# 2. 记录所有 ROS2 topic
ros2 topic list > topics.txt

# 3. 确认关键 topic 类型
ros2 topic info /image_rgb
ros2 topic info /imu
ros2 topic info /scan
ros2 topic info /camera/aligned_depth_to_extcolor/image_raw
ros2 topic info /camera/depth/image_rect_raw

# 4. 确认图像编码
ros2 topic echo /image_rgb --once | grep encoding

# 5. 检查 LCM 通道
lcm-spy

# 6. 测试 LLM 链路
ros2 service call /llm_ask cyberdog_race/srv/LLMAsk "{prompt: 'hello'}"
```

---

## 七、上机注意事项 ⚠️

### 7.1 相机 lifecycle — 不激活就没有图像

真狗的双目/RGB 相机和 Realsense D435 都通过 ROS2 lifecycle 状态机管理，**必须先激活才能发布 topic**。开机后相机默认是 `unconfigured` 状态，不发任何数据。

```bash
# ── 双目/RGB 相机（/image_rgb, /image_left, /image_right） ──
ros2 lifecycle set /stereo_camera configure
ros2 lifecycle set /stereo_camera activate

# ── Realsense D435（/camera/depth/..., /camera/infra1/..., /camera/imu） ──
ros2 lifecycle set /camera/camera configure
ros2 lifecycle set /camera/camera activate

# ── D435 align 节点（/camera/aligned_depth_to_extcolor/image_raw） ──
ros2 lifecycle set /camera/camera_align configure
ros2 lifecycle set /camera/camera_align activate
```

> 验证：激活后 `ros2 topic list | grep -E 'image_rgb|aligned_depth'` 应该有输出。

### 7.2 LCM 多播路由 — 不配路由收不到数据

真狗 LCM 走 UDP 多播（`239.255.76.67`），外部 PC 需要通过网线连接狗并配置多播路由表，否则收不到 `global_to_robot`（里程计）和 `motor_temperature` 等 LCM 数据。

```bash
# 官方脚本（位于 loco_hl_example 仓库）
./auto_lcm_init.sh

# 或手动添加
sudo route add -net 239.255.0.0 netmask 255.255.0.0 eth0
```

> 验证：`lcm-spy` 能看到 `global_to_robot`、`motor_temperature` 等通道。

### 7.3 消息类型陷阱

| 话题 | 实际类型 | 常见错误 |
|---|---|---|
| `touch_status` | `protocol::msg::TouchStatus`（含 Header） | ❌ 用 `std_msgs::msg::Int32` 会反序列化崩溃 |
| `bms_status` | `protocol::msg::BmsStatus`（多字段） | ❌ 用 `Float32MultiArray` 只能读到乱序数据 |
| `/scan` | **可能**是 `ScanMsg` 而非 `sensor_msgs/LaserScan` | ❌ 直接 `LaserScan` 订阅失败 |
| `head_tof_payload` | `protocol::msg::HeadTofPayload` | ❌ 无此类型编译不过 |

> 解决：从真狗 `/opt/ros2/cyberdog/share/bridges/` 下获取 `protocol` 消息包。

### 7.4 真狗状态机保护 — 低电量会自动禁止运动

真狗 `cyberdog_manager` 会强制状态机切换：

| 电量 | 状态机 | 影响 |
|---|---|---|
| ≤20% | **Protect** | 禁止空翻等剧烈运动 |
| ≤5% | **LowPower** | 强制趴下，**所有运动指令被忽略** |
| 0% | **TearDown** | 关机 |

> 我们的 BMS 监控在 ≤20% 时会主动停止比赛，比状态机更早介入。

### 7.5 运动控制优先级

`motion_manager` 内部优先级（值越小越高）：**App(0) > Audio(1) > Vis(2) > BluTele(3) > Algo(4)**

> LCM `gamepad_lcmt` 被视为蓝牙手柄（BluTele=3）。如果 APP 也在发控制指令（App=0），比赛指令可能被覆盖。

### 7.6 电机温度保护

| 阈值 | 行为 |
|---|---|
| 74°C | 禁止切换到空翻模式 |
| 82°C | 线圈温度过高警告 |
| 87°C | 电机自动保护（Coil Thermal-Shutdown） |

> 我们的电机温度监控框架（`MotionCtrl` 内）检测到 >74°C 会标记 `overheat`。

### 7.7 `life_count` 必须递增

LCM `robot_control_cmd` 的 `life_count` 必须每帧递增（0→255 循环），否则运控板会忽略重复指令并触发 500ms 超时保护（自动趴下）。

> ✅ 我们的 `MotionCtrl::pub_lcm_cmd()` 已正确实现 `lcm_cmd_.life_count = ++lcm_life_`。

### 7.8 SSH 连接方式

```bash
# 有线（USB-TypeC Download 口）
ssh mi@192.168.55.1   # 密码: 123

# 无线（APP 内查看 IP）
ssh mi@<真狗IP>
```
