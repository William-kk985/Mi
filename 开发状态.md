# cyberdog_race 开发状态总览

> 更新日期: 2026-07-07

---

## 一、编译宏总览（`debug_config.hpp`）

| 宏 | 状态 | 说明 |
|---|---|---|
| `DEBUG_VISION` | 🔒 注释 | 弹窗可视化（cv::imshow） |
| `DEBUG_MOTION` | 🔒 注释 | 运动指令日志 |
| `DEBUG_SENSOR` | 🔒 注释 | 传感器数据日志 |
| `DEBUG_STAGE` | 🔒 注释 | 状态机切换终端打印 |
| `DEBUG_SINGLE_STAGE` | 🔒 注释 | 只跑单个赛段 |
| `DEBUG_START_STAGE` | 🔒 注释 | 从指定赛段开始 |
| `DEBUG_END_STAGE` | 🔒 注释 | 跑完指定赛段停止 |
| `DEBUG_NO_HIT` | 🔒 注释 | 禁用撞球 |
| `DEBUG_TEST_BEHAVIOR` | 🔒 注释 | 行为测试模式 |
| `ENABLE_WEB_STREAMING` | 🔒 注释 | **Web 推流总开关** |
| `REAL_DOG` | 🔒 注释 | **仿真/真机模式切换** |
| `WEB_STREAM_PORT` | ✅ 8080 | 推流端口号 |
| `TOPIC_RGB_CAMERA` | 见模式 | RGB 相机 topic |
| `TOPIC_IMU` | 见模式 | IMU topic |
| `TOPIC_LIDAR` | 见模式 | LiDAR topic |
| `TOPIC_D435` | 见模式 | D435 相机 topic |
| `TOPIC_ODOM` | 仅真机 | ROS2 里程计 topic |
| `LCM_STATE_ESTIMATOR` | 仅真机 | LCM 状态估计通道 |
| `LCM_CMD_EXEC` | 仅真机 | LCM 参数下发通道 |

---

## 二、已实现功能 ✅

### 2.1 Web 推流 — 嵌入式 MJPEG 服务

| 功能 | 文件 | 说明 |
|---|---|---|
| MJPEG HTTP 服务器 | `web_streamer.hpp/cpp` | POSIX socket 实现，零外部依赖 |
| 多客户端支持 | 同上 | 每连接独立线程，默认 max 4 |
| `/stream` — 原始相机 | 同上 | 640×480, 30fps, JPEG 质量 70 |
| `/stream/debug` — 标注画面 | 同上 | 黄线边界、球圈、传感器文字叠加 |
| `/stream/lidar` — 雷达俯视图 | 同上 | 240×240, 5fps, 距离环+红近绿远扫描点 |
| `/stream/track` — 里程轨迹图 | 同上 | 240×240, 5fps, 历史轨迹+当前朝向箭头 |
| `/stream/telemetry` — 遥测仪表盘 | 同上 | 320×240, 5fps, yaw罗盘+身高柱+赛段状态 |
| `/stream/d435` — D435 相机 | 同上 | 640×480, 30fps, JPEG 质量 65 |
| `/api/telemetry` — JSON 遥测 | 同上 | 占位返回 `{"stage":0}`，供前端顶部栏轮询 |
| 交互式 Web 前端 | 同上 | 左固定原始+右下拉切换（🔍标注/📡雷达/🗺️轨迹/📊遥测/📏D435）；⛶ 放大/还原；FPS 计数器 |
| 编译宏控制 | `debug_config.hpp` | `ENABLE_WEB_STREAMING` 注释=零开销 |

### 2.2 真机适配框架

| 功能 | 文件 | 说明 |
|---|---|---|
| 仿真/真机双模式 | `debug_config.hpp` | `REAL_DOG` 宏切换 |
| Topic 名宏化 | `race_controller.cpp` | 5 个传感器 topic 用 `TOPIC_*` 宏，两套值 |
| ROS2 `/odom` 订阅 | `race_controller.cpp` | `on_odom()` 真机里程计回调 |
| LCM `state_estimator` 订阅 | `race_controller.cpp` | `on_state_estimator()` 真机状态估计回调 |
| LCM 参数下发框架 | `race_controller.cpp` | `apply_stage_params()` 末尾伪代码占位 |
| LCM `simulator_state` 保留 | `race_controller.cpp` | 仿真模式继续使用 |

### 2.3 代码质量修复

| # | 文件 | 问题 | 修复 |
|---|---|---|---|
| P0 | `web_streamer.cpp` | `stop()` 阻塞在 `accept()` | `shutdown(server_fd_, SHUT_RDWR)` 强制唤醒 |
| P1 | `web_streamer.cpp` | 客户端计数器泄漏 | `active_clients_` 原子计数，三路退出均递减 |
| P2 | `motion_ctrl.cpp` | `set_step_height` 用错 LCM 端口 | 改为 `ctrl_lcm_.publish()` (7671) |
| P4 | `web_streamer.cpp` | `send_header` 格式化溢出 | `snprintf` 返回值校验再 write |
| P7 | `web_streamer.cpp` | `read_line` 慢速攻击 | 加 4096 字节上限 |
| — | `race_controller.cpp` | `on_d435` unused param 警告 | `(void)msg` + `#ifdef` 守卫 |
| — | `race_controller.cpp` | narrowing conversion 警告 | `cv::Scalar(double,double,double)` |
| — | `CMakeLists.txt` | `state_estimator_lcmt.hpp` 找不到 | 加 `cyberdog_visual/lcm_type` include 路径 |

---

## 三、未实现 / 待完成 ⏳

### 3.1 Web 前端

| 项目 | 状态 | 说明 |
|---|---|---|
| 顶部状态栏实时数据 | 🔴 空壳 | `/api/telemetry` 返回占位 JSON，需接到真实 `SensorData` |
| 里程轨迹 Tab 显示 | 🟢 已实现 | 5fps 渲染，自动缩放范围 |
| 遥测仪表 Tab | 🟢 已实现 | yaw罗盘 + 身高柱 + 赛段参数 |
| LiDAR 俯视图 Tab | 🟢 已实现 | 距离环 + 红近绿远扫描点 |
| D435 深度图伪彩色 | 🔴 未做 | 当前只推流 D435 color 原图，未做深度伪彩渲染 |
| 四足接触状态显示 | 🔴 未做 | `cyberdog_visual/lcm_type` 有 `leg_control_data_lcmt`，可读取 `q[12]/tau_est[12]/force_est[12]` |
| 命令发送按钮（跳过赛段/暂停） | 🔴 未做 | 需要前端 JS → HTTP POST → C++ 回调的安全链路 |

### 3.2 真机适配（需 SSH 进狗确认）

| 项目 | 状态 | 确认方式 |
|---|---|---|
| 5 个传感器 topic 精确名称 | 🟡 宏已占位 | `ros2 topic list` 后替换 `debug_config.hpp` 中的 TODO 字符串 |
| 图像编码格式 (bgr8 vs rgb8) | 🔴 待确认 | 订阅 topic 后用 `ros2 topic echo` 看 encoding 字段 |
| `use_rc=0` 是否生效 | 🔴 待确认 | 真机发 LCM `gamepad_lcmt` 后看狗是否有反应 |
| 参数下发通道 | 🔴 待确认 | 查真狗 `motion` 模块是收 ROS2 `yaml_parameter` 还是 LCM `control_parameter_lcmt` |
| `body_height` 来源 | 🟡 假设置 `state_estimator.p[2]` | 真机对比 `state_estimator.p[2]` 与实际身高看差值 |
| `lidar_ranges` topic 是否还是 180 点 180° | 🔴 待确认 | 真狗可能有多个雷达 topic，角度范围可能不同 |

### 3.3 赛段逻辑（赛题实现）

| 赛段 | 状态 | 备注 |
|---|---|---|
| Stage1 石径探路 | 🟢 可运行 | 视觉/IMU融合巡线 |
| Stage2 荒野寻珠 | 🟢 可运行 | S型路径点导航 |
| Stage3 曲道冲锋 | 🟡 待调参 | 路径点导航+视觉微调 |
| Stage4 深隧寻珍 | 🔴 空壳 | 30+状态状态机，未对接视觉 |
| Stage5 孤梁稳渡 | 🔴 框架 | 独木桥居中，未实现 |
| Stage6 撷金建功 | 🔴 框架 | YOLO+Lidar，未实现 |

### 3.4 视觉算法

| 模块 | 状态 | 说明 |
|---|---|---|
| LaneDetector (HSV 巡线) | 🟢 完成 | STRICT/RELAXED 双模式 |
| BallDetector (HSV 球检测) | 🟢 完成 | 橙/蓝/白三色 |
| Stage4Detector (Hough 圆) | 🟡 框架 | 5 种目标检测，未对接状态机 |
| YOLO 目标检测 | 🔴 占位 | `yolo_detector.hpp` 只有 TODO 注释 |
| LiDAR 定位 | 🔴 占位 | `lidar_locator.hpp` 只有 TODO 注释 |

---

## 四、文件清单

### 新增文件

| 文件 | 说明 |
|---|---|
| `include/cyberdog_race/vision/real/web_streamer.hpp` | WebStreamer 类声明 |
| `src/vision/real/web_streamer.cpp` | WebStreamer 完整实现（~525行） |
| `include/cyberdog_race/vision/real/yolo_detector.hpp` | 占位 |
| `include/cyberdog_race/vision/real/lidar_locator.hpp` | 占位 |
| `include/cyberdog_race/stages/real/stage4_real.hpp` | 占位 |
| `include/cyberdog_race/stages/real/stage5_real.hpp` | 占位 |
| `include/cyberdog_race/stages/real/stage6_real.hpp` | 占位 |

### 修改文件

| 文件 | 本次会话改动要点 |
|---|---|
| `debug_config.hpp` | `ENABLE_WEB_STREAMING`, `REAL_DOG`, topic 宏 |
| `CMakeLists.txt` | `cyberdog_visual/lcm_type` include 路径, `web_streamer.cpp` |
| `race_controller.hpp` | `nav_msgs/Odometry`, `state_estimator_lcmt`, `on_odom()`, `on_state_estimator()`, `sub_odom_`, `odom_history_` |
| `race_controller.cpp` | topic 宏化, ROS2 odom 订阅, LCM 双模式, LiDAR/轨迹/遥测渲染, D435 推流, 真机参数伪代码 |
| `sensor_data.hpp` | `lidar_ranges` 向量 |
| `motion_ctrl.cpp` | `set_step_height` LCM 端口修复 |
| `web_streamer.hpp` | 6 帧缓冲, 3 种 push 方法, `server_fd_`, `active_clients_` |
| `web_streamer.cpp` | 全量重写: 交互式前端, 6 路流路由, 多客户端, P0-P7 修复 |

---

## 五、API 路由总表

| 路径 | MIME | 帧率 | 说明 |
|---|---|---|---|
| `/` | `text/html` | — | 交互式调试面板 |
| `/stream` | `multipart/x-mixed-replace` | 30Hz | 原始 RGB 相机 |
| `/stream/debug` | 同上 | 30Hz | 标注画面 |
| `/stream/lidar` | 同上 | 5Hz | LiDAR 俯视雷达图 |
| `/stream/track` | 同上 | 5Hz | 里程轨迹图 |
| `/stream/telemetry` | 同上 | 5Hz | 遥测仪表盘 |
| `/stream/d435` | 同上 | 30Hz | D435 深度相机 |
| `/api/telemetry` | `application/json` | 轮询 | 遥测数据（占位） |

---

## 六、真机 SSH 检查清单

```bash
# 1. 记录所有 ROS2 topic
ros2 topic list > topics.txt

# 2. 查看每个 topic 的消息类型
ros2 topic info /camera/color/image_raw
ros2 topic info /imu
ros2 topic info /scan
ros2 topic info /odom

# 3. 查看图像编码
ros2 topic echo /camera/color/image_raw --once | grep encoding

# 4. 检查 LCM 通道
lcm-spy  # 观察有哪些活跃通道

# 5. 测试 LCM 运动控制
# 写简单脚本发 gamepad_lcmt，看狗是否有反应
```

---

## 七、编译命令

```bash
# 仿真模式（默认）
colcon build --merge-install --packages-select cyberdog_race

# 真机模式（先取消注释 #define REAL_DOG）
colcon build --merge-install --packages-select cyberdog_race

# Web 推流模式（先取消注释 #define ENABLE_WEB_STREAMING）
colcon build --merge-install --packages-select cyberdog_race
```
